- content_for :nav_content do
  %h1 Left Nav Content

- content_for :main_content do
  - @posts.each do |post|
    = render partial: 'posts/post', locals: {post: post}

  .text-center
    -# = will_paginate @posts
