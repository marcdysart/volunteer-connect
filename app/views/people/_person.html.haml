= content_tag :div, class: 'media', id: "person-#{person.id}" do
  .media-body
    %p
      = link_to person.name, person, class: 'tag label label-info'
      - if policy(person).destroy?
        | #{link_to "Remove Person", [person.post, person], method: :delete, remote: true}
